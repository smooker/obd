# TEST_RUN — ASX 4N13, DS150E Bluetooth, 2026-04-30

Първи live тест на DS150E директно от Linux без Windows VM.
Кола: Mitsubishi ASX 2014, 1.8 DiD (4N13), AWD. Двигателят е бил пален.

---

## 1. Transport

| | |
|---|---|
| Физически | Bluetooth BR/EDR, SPP профил |
| RFCOMM channel | 3 |
| Device | `/dev/rfcomm0` |
| Baud (serial) | 115200 8N1, DTR+RTS high |
| BT MAC | 21:61:5C:AA:AC:65 |

Конекцията е стабилна в движение. BT не се губи при шум/вибрация.

---

## 2. Init последователност (потвърдена работеща)

```
*607              → затваря евентуална стара сесия (ЗАДЪЛЖИТЕЛНО преди *668)
*668_0_500_7E0_7E8_000_01C_02_3E_02_00_00_00_00_00_080_1A_87
                  → отваря CAN сесия 500 kbps, txid=7E0, rxid=7E8
                  → отговаря с ECU part number: 1860C481
```

**Критично:** `*607` трябва да се изпрати преди всяка `*668`. Без него `*668` виси
безкрайно ако вече има активна сесия.

ECU отговор от `*668` — decimal ASCII bytes за "1860C481":
```
49 56 54 48 67 52 56 49
```

---

## 3. Четене на параметри

Команден формат:
```
*608_21_<idx_hex>
```

Отговор:
```
*97 <idx_hex> <b0> <b1> <b2> ...   (decimal bytes, space-separated)
```

Функция в lib: `ds_ecu_read_param(idx, data, datasz)` → брой байтове

---

## 4. Capture: captures/live_20260430_072226.csv

- 81 цикъла × 9 параметъра
- Всички стойности са замръзнали (идентични) → CAN сесията вероятно не е
  reinit-ната правилно; ECU е отговарял с кеширан snapshot вместо live данни.
- Въпреки това форматът и комуникацията работят. Raw стойностите са валидни
  за интерпретация.

### Raw стойности от capture (константни):

| Param | Raw | Бележка |
|-------|-----|---------|
| 0x02 b1 | 17 → **-23°C coolant** | b-40 decode, но стойността е нереалистична за работещ двигател → ECU snapshot от студен старт |
| 0x02 b2 | 4 → **-36°C intake** | нереалистично → snapshot |
| 0x03 b0/b1 | 0/5 → **RPM=5** | нереалистично → snapshot |
| 0x04 b0 | 33 → load_raw=33 | — |
| 0x04 b2 | 0 → throttle=0 | — |
| 0x74 b0 | 6 → **DPF soot ~6%** | нисък, очаквано за здрав DPF |
| 0x74 b4/b5 | 4/0 → dp_raw=1024 | диф. налягане, формула неизвестна |
| 0x11 b2/b3 | 19/146 → **dist_regen=5010 km** | км от последна регенерация |
| 0x16 b0/b1 | 96/153 | неизвестно |
| 0x19 b0 | 16 | неизвестно |
| 0x24 b0/b1 | 9/7 или 9/8 | неизвестно, вероятно rail pressure |
| 0x51 b0 | 24 (0x18) | неизвестен флаг |

---

## 5. DTC

Команда: `*608_18_00_FF_00`  
Отговор: `*88 0` — **0 активни грешки**. Колата е чиста.

---

## 6. VIN

Опит: `*609` с service $09, PID $02.  
Резултат: `serviceNotSupportedInActiveSession`  
→ VIN не е достъпен по този path. Нужно: capture от Autocom при VIN четене,
   или директен UDS $22 DID 0xF190 след правилна сесия.

---

## 7. ECU Address Scan (ecu_scan)

Сканирани 23 адреса: 0x700..0x7DF (стъпка 0x10) + 0x7E0..0x7E7.

**Резултат: само 0x7E0/0x7E8 отговаря** — Engine ECU.

Останалите (ABS 0x7B0, BCM 0x760, ETACS 0x780, SRS 0x7D0 и др.) — няма отговор
на стандартен `*668` с offset +8. Body ECU вероятно изискват различен протокол
(MUT-II / K-line) или различни `*668` параметри. Нужен Autocom capture при
достъп до тях.

---

## 8. Известни параметри (потвърдени)

| Idx | Байтове | Decode | Статус |
|-----|---------|--------|--------|
| 0x02 | b1, b2 | coolant_C = b1-40, intake_C = b2-40 | потвърден |
| 0x03 | b0, b1 | RPM = b0×256+b1 | потвърден формат, scaling нуждае се верификация |
| 0x04 | b0, b2 | load_raw, throttle_raw | потвърден |
| 0x11 | b2, b3 | dist_since_regen_km = b2×256+b3 | потвърден |
| 0x17 | b0, b1 | 64 64 = 100,100 (% ?) | непотвърден |
| 0x46 | ASCII | "/6Ruk" — инжектор ID фрагмент | от USB capture |
| 0x4F | ASCII | "77/H" — инжектор ID фрагмент | от USB capture |
| 0x5B | ASCII | "AAA6" — инжектор ID фрагмент | от USB capture |
| 0x74 | b0, b4/b5 | dpf_soot_raw, dpf_dp_raw | формула неизвестна |
| 0xB0-0xB9 | 2 байта | injector qty correction (10 work points) | от USB capture |

### Неизвестни (raw данни събрани, интерпретация предстои):

| Idx | Raw (от capture) | Кандидат |
|-----|-----------------|---------|
| 0x16 | 96/153 | boost pressure? EGR? |
| 0x19 | 16 | flags? режим? |
| 0x24 | 9/7 | fuel rail pressure (low = студена ECU) |
| 0x51 | 0x18 | статус байт |
| 0x08, 0x09, 0x10, 0x12 | — | от USB capture, не четени в live |

---

## 9. Injector QR код

От параметри 0x46, 0x4F, 0x5B — ASCII фрагменти от инжекторния QR код.
DENSO инжектор: 295050-0120 / 1465A323. QR е 30 символа.
Пълен QR не е reconstruct-нат — нужен scan на всички ASCII параметри.

---

## 10. Calibration lookup (A0-A8 серия)

Параметрите 0xA0, 0xA1, 0xA3, 0xA4, 0xA7, 0xA8 дават X→121 двойки от USB
capture. Вероятно са калибрационни таблици (впръскване, налягане).
Не са четени в live режим.

---

## 11. Следващи стъпки

1. **Замразените данни** — добави `*607` + `*668` reinit преди всеки logging
   цикъл, или провери дали ECU връща нов snapshot при всяка `*608_21_XX`.

2. **Body ECU (ABS/SRS/ETACS/AWC)** — нужен Autocom capture докато
   Windows VM достъпва тези системи. Не знаем: правилните CAN адреси,
   дали изобщо е CAN или MUT-II K-line, `*668` параметрите.

3. **RPM scaling** — при следващ тест на газ вземи RPM на известни обороти
   (напр. 2000 rpm на тахометъра) и провери формулата b0×256+b1.

4. **DPF decode** — dpf_soot=6 изглежда реалистично (6%). dp_raw=1024
   нуждае се от referentна стойност при знайно налягане.

5. **Пълен параметър scan** — при следващ тест изпрати `scan` команда
   (всички 50+ параметъра) за да хванем всичко наведнъж.

6. **VIN** — UDS $22 DID 0xF190 след `*609` ExtendedDiagnostic сесия.
